# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()
GET     /restricted                 controllers.Application.restricted()

GET     /login                      controllers.Application.login()
GET     /logout                     com.feth.play.module.pa.controllers.Authenticate.logout
GET     /authenticate/:provider     com.feth.play.module.pa.controllers.Authenticate.authenticate(provider: String)

GET     /accounts/add               controllers.Application.add()
GET     /accounts/link              controllers.Application.link()
POST    /accounts/link              controllers.Application.doLink()

GET     /accounts/merge             controllers.Application.merge()
POST    /accounts/merge             controllers.Application.doMerge()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
